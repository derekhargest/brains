function calculateSum(numbers) {
    let sum = 0;
    // Missing the loop to add numbers
    return sum;
  }
  
  const result = calculateSum([1, 2, 3, 4, 5]);
  console.log(`Sum: ${result}`); // Should output 15, but will output 0